<h1>Products</h1>

<a href="products/new" style="color:red ; font-size:24px ; font-style:italic ; font-style:bold"> Thêm </a>

<div id="products">
  <% @products.each do |product| %>
    <ul>
        <li style="list-style:none">
            <a href="/products/<%= product.id %>" style="color:red ; font-size:24px ; font-style:italic ; font-style:bold"> <%= product.name %> </a>
            <p style="font-size:14px ; margin-left: 16px">  <%= product.title %></P>
        </li>
    </ul>
  <% end %>
</div>
