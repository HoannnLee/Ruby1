<h1 class="page-title">Products</h1>

  <a class="btn" href="products/new">Thêm</a>

  <div id="products">
    <ul class ="block">
      <% @products.each do |product| %>
          <li class="product-item">
            <a class="product-link" href="/products/<%= product.id %>">
              <%= product.name %>
            </a>
            <p class="product-price"><%= product.price %></p>
          </li>
      <% end %>
    </ul>
    </div>
